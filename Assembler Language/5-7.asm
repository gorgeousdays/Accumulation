DATAS SEGMENT
DATA DW 1,3,5,2,4,8,2,12,58,8,33,14,11,20,'$'
Z DB 100
DATAS ENDS

STACKS SEGMENT
DW 100 DUP(?)
STACKS ENDS

CODES SEGMENT
START:
MOV AX,DATAS
MOV DS,AX
LEA BX,DATA
MOV DX,[BX]
MOV AX,4        

TEST1: MOV CX,DX    
RCR CX,1    
JNC COMP  
JC CONTINUE  ;奇数跳转 

COMP:
CMP AX,DX  
JL  CONTINUE     ;小于跳转
JNL CHANGE

CHANGE:
MOV AX,DX
JMP CONTINUE 

CONTINUE:
ADD BX,2
MOV DX,[BX]
CMP DX,24H    ;与结束符$比较
JNZ TEST1     ;不相等重来
JMP SHUCHU  


SHUCHU:
DIV Z

PUSH AX
CMP AL,0  

JZ AA
ADD AL,30H   

MOV DL,AL
MOV AH,02H
INT 21H   

AA:    
POP AX
MOV AL,0
ADD AH,30H   

MOV DL,AH
MOV AH,02H
INT 21H


CODES ENDS
END START
